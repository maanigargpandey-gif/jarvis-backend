name: jarvis_backend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
    hive_flutter: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # नोट: सभी एसेट लाइन्स हटा दी गई हैं क्योंकि फाइलें अभी मौजूद नहीं हैं।
  # जब तुम इमेज फोल्डर बना लोगे, तब हम उन्हें वापस जोड़ेंगे।
  
